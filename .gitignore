# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Secrets — never commit API keys or local config
.env
.env.local
*.key
secrets.json
secrets.local.js

# Logs
*.log

# Node (in case we add tooling later)
node_modules/

# Claude Code local settings
.claude/settings.local.json

# Legacy local-only queue file (superseded by in-extension training queue)
to-train.json
